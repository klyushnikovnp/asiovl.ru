<div{{ attributes }}>
  {{ title_prefix }}
  {% if name and not page %}
    <h2><a href="{{ url }}">{{ name }}</a></h2>
  {% endif %} 
  {{ title_suffix }}
  {{ content|without('field_cover_catalog', 'description') }}
</div>